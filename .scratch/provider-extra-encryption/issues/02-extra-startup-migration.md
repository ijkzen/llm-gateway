# 02: 启动时一次性迁移历史明文 extra

**What to build:** 服务启动早期（请求进来前）扫描全部 provider 行，把无 `enc:v1:` 前缀的明文 extra 加密写回。未配置 `API_KEY_ENCRYPTION_KEY` 时跳过迁移（明文降级 + 日志说明，与现有 api_key 行为一致），配置密钥后下次启动自动完成。幂等可重试：已加密行跳过。单行迁移失败（历史数据损坏等）跳过该行加 warn，不阻塞启动与其余行。

**Blocked by:** 01: 供应商 extra 写入加密、读取解密全链路

**Status:** ready-for-agent

- [ ] 明文 extra 行 → 迁移后为密文且可解密回原文
- [ ] 已加密行跳过（幂等，重复执行无变化）
- [ ] 未配置密钥时跳过迁移
- [ ] 单行失败跳过 + warn，其余行正常迁移，不阻塞启动
