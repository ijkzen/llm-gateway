# 02: 页面成员卡片点击打开详情弹窗

**What to build:** 从用户视角，虚拟模型页面里每个成员卡片都能被点击——无论该成员正常、已停用、随供应商禁用，还是所在虚拟模型已停用——点击后弹出只读详情弹窗。编辑弹窗内不做此改动。

**Blocked by:** 01（成员只读详情弹窗，含启停开关）

**Status:** ready-for-agent

- [ ] `VirtualModelSection` 成员卡片改为可点击（保留现有禁用降透明度样式），新增点击上抛回调；页面持有「被点击成员 + 所属虚拟模型」状态。
- [ ] 四种成员状态（正常 / 已停用 / 随供应商禁用 / 虚拟模型停用）下点击均能打开详情弹窗。
- [ ] 页面测试（红→绿）：mock 详情弹窗，逐状态点击卡片断言弹窗 `open` 置真并传入正确的目标成员与虚拟模型；关闭回调复位状态。
