# 05: 规格与工单 docs（docs 提交）

**What to build:** 本特性的需求 / 规格 / 工单文档落库并随 `docs:` 提交，供后续追溯。
（仓库约定 `.scratch/<feature-slug>/` 内容为提交物，历史见 `1c46b07` 等 docs 提交。）

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] `.scratch/api-key-overview-nav/REQUIREMENTS.md`（grill 产出）已就位
- [ ] `.scratch/api-key-overview-nav/spec.md`（to-spec 产出）已就位
- [ ] `.scratch/api-key-overview-nav/issues/01..04` 工单文件已就位
- [ ] 以 `docs: api key overview nav requirements, spec and tickets` 单独提交（不混入实现 commit）
