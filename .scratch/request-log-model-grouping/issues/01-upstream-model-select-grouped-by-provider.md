# 01: 上游模型过滤下拉按供应商分组

**What to build:** 请求日志页「上游模型」过滤下拉不再平铺模型 ID，而是按供应商分组展示：每个供应商一个分组（供应商名为分组标题），组内列出该供应商的模型。未选供应商时所有供应商分组按供应商列表顺序依次展示，同名模型分属各自供应商组、一眼可辨归属；选中某供应商后仅剩该供应商一组（现有级联行为不变）；「全部」选项保留在最前。供应商列表查不到的条目分组标题兜底 `#<providerId>`。选中模型名仍跨供应商匹配（后端零改动）。

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [x] 未选供应商时打开模型下拉：出现多个按供应商名命名的分组，同名模型出现在各自供应商组下
- [x] 选中某供应商后打开模型下拉：只剩该供应商一组，组内为该供应商模型
- [x] 「全部」选项保留在所有分组之前；选中模型后筛选请求行为与现状一致
- [x] `pnpm lint` / `pnpm vitest run` 全绿
